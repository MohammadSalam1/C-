#include <iostream>
#include <string>
#include <vector>
using namespace std;

int main(){

/*
    vector<int> nums = {1, 2, 3, 4, 5};

    for (int i : nums) {
        cout << i << " ";
    }


    array<int, 3> nums = {2, 4, 6};

    for (int i = 0; i < nums.size(); i++) {
        nums[i] *= 2; // double the value
    }

    for (int val : nums) {
        cout << val << " ";
    }
    // Output: 4 8 12


    list<string> letters = {"a", "b", "c"};

    auto it = letters.begin(); // points to "a"
    ++it;                      // now points to "b"
    letters.insert(it, "x");   // insert "x" before "b"

    for (string& s : letters) {
        cout << s << " ";
    }
    // Output: a x b c


    for (int& i : nums) {
        i += 10;
    }

    for (int i : nums) {
        cout << i << " ";
    }
    // Output: 11 12 13
    
*/




return 0;
}